# Root .env.example — shared keys for the compose file and services.
# No secrets exist in this project (static landing page, no backend, no database).

# Frontend (used by docker-compose.yml)
NODE_ENV=development
NEXT_PUBLIC_API_URL=http://localhost:3000
